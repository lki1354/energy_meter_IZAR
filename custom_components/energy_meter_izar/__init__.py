"""Energy Meter IZAR — M-Bus gateway (HC2XML over FTP) integration.

Phase 1 ships the pure-Python decoding library (`mbus_parser`, `ingest`);
the Home Assistant integration entry points follow in phase 2.
"""
